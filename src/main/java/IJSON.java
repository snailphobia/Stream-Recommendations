public interface IJSON {
    /**
     * @return String
     * will return the object as a JSON string
     * this method may be used both individually and within a list of objects 
     */
    public String toJSON();
}
